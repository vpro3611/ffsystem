<Window x:Class="FileSystemP.WPF.Views.ContentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New File with Content" Width="400" Height="220"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="File name:" Margin="0,0,0,2"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,0,0,8"/>
        <Label Grid.Row="2" Content="Content:" Margin="0,0,0,2"/>
        <TextBox x:Name="ContentBox" Grid.Row="3" AcceptsReturn="True" Height="60"
                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="72" Margin="0,0,8,0" IsDefault="True" Click="OK_Click"/>
            <Button Content="Cancel" Width="72" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
